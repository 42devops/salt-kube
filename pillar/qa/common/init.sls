hw:
  #the default interface
  netiface: 'eth0'
# the cluster domain name used for internal infrastructure host <-> host  communication
internal_infra_domain: 'infra.devops.prod'
ldap_internal_infra_domain: 'dc=infra,dc=devops,dc=prod'
